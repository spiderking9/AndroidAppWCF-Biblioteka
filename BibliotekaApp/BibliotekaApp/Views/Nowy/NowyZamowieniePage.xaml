<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BibliotekaApp.Views.Nowy.NowyZamowieniePage"
             Shell.PresentationMode="ModalAnimated"
             Title="Nowy Zamownienie">
    <ContentPage.Content>
        <StackLayout Spacing="3" Padding="15">
            <Label Text="IdKsiazki" FontSize="Medium" />
            <Entry Text="{Binding IdKsiazki, Mode=TwoWay}" FontSize="Medium" />
            <Label Text="IdPracownika" FontSize="Medium" />
            <Editor Text="{Binding IdPracownika, Mode=TwoWay}" AutoSize="TextChanges" FontSize="Medium" Margin="0" />
            <Label Text="IdKsiegarni" FontSize="Medium" />
            <Entry Text="{Binding IdKsiegarni, Mode=TwoWay}" FontSize="Medium" />
            <Label Text="RokWydania" FontSize="Medium" />
            <Entry Text="{Binding RokWydania, Mode=TwoWay}" FontSize="Medium" />
            <StackLayout Orientation="Horizontal">
                <Button Text="Anuluj" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Zapisz" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
